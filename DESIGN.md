# 뮤지컬 〈이끼숲〉 관극 다이어리 — 설계서

작성일 2026-09-05 · 베이스 코드 [`musical-otk/widerstand2026`](https://github.com/musical-otk/widerstand2026)

---

## 1. 개요

천선란 소설 원작 창작 뮤지컬 〈이끼숲〉(2026.10.01–12.13, 백암아트홀)용 관극 기록 PWA.
`widerstand2026`을 베이스로 **배역 체계·공연 일정·극장 좌석·특전 일정**을 교체한다.

베이스 앱이 이미 제공하는 기능(그대로 승계):

| 화면 | 기능 |
| --- | --- |
| 홈 | 다음 공연 안내, 관람 요약 |
| 캘린더 | 배역별 캐스팅 필터, 회차 표시, 관람 기록 |
| 쿠폰/도장 | 할인권 잔여 관리, 도장판 적립 |
| 통계 | 배우별·조합별 관람 횟수, 좌석 히트맵 |
| 설정 | 데이터 백업/복원 |

저장소는 브라우저 localStorage(기기 로컬, 기기 간 동기화 없음).

---

## 2. 자료 확보 현황

### 2.1 확인 완료

`show-info.json`에 기록해 둠.

| 항목 | 값 | 출처 |
| --- | --- | --- |
| 공연 기간 | 2026-10-01 ~ 2026-12-13 | 언론 보도 |
| 극장 | 백암아트홀 | 언론 보도 |
| 원작 | 천선란 『이끼숲』(2023) | 언론 보도 |
| 극본 / 작곡 / 연출 | 박해림 / 리카C / 오루피나 | 언론 보도 |
| 공식 X | [@_LIVECONNECTION](https://x.com/_LIVECONNECTION) | 사용자 제공 |
| 예매처 | 티켓링크 상품 65293 | 사용자 제공 |

**배역 5종 / 배우 15명**

| 키 | 배역 | 설명 | 배우 |
| --- | --- | --- | --- |
| `marco` | 마르코 | 클론연구소 경비원 | 송유택 · 현석준 · 홍승안 |
| `soma` | 소마 | 도시의 모든 소리를 듣는 도시감청팀 | 강연정 · 강혜인 · 전혜주 |
| `yuo` | 유오 | 지상 숲이 실재한다 믿는 개발건설팀 | 정욱진 · 이주순 · 이한솔 |
| `uiju` | 의주 | 원칙주의자, 도시계획팀 | 정다예 · 이혜진 · 오유민 |
| `eunhui` | 은희 | 마르코의 동료 | 김청아 · 한비 · 윤지우 |

> 베이스는 6배역, 이끼숲은 **5배역**. 배역 하나가 줄어드는 구조 변경이 필요하다.

**공연 정보** (공식 INFORMATION 이미지)

| 항목 | 값 |
| --- | --- |
| 공연시간 | 화~금 20:00 / 토·공휴일 15:00, 19:00 / 일 14:00, 18:00 |
| 예외 | 2026-10-05(월) 15:00 — 월요일 공연은 이 회차뿐 |
| 러닝타임 | 100분 (인터미션 없음) |
| 관람연령 | 8세 이상 (미취학 아동 관람 불가) |
| 티켓가격 | R석 77,000원 / S석 66,000원 |
| 좌석 | 1층 A~M열 (R석) · 2층 N~R열 (S석), 1층 M열 우측 휠체어석 |
| 기획/제작 | (주)라이브커넥션 · 홍보마케팅 노아드 |

**캐스팅 스케줄** — 프리뷰·1차 오픈분 **39회차** (2026-10-01 ~ 11-01) 전량 확보,
`schedule.json`에 기록하고 `scripts/validate.py`로 검증 완료.

**특전** — `events.json`에 기록

| 이벤트 | 기간 |
| --- | --- |
| 프리뷰 기간 (50% 할인) | 10/1 ~ 10/8 |
| 웰컴 키트 증정 | 10/1 ~ 10/11 (적용회차 10/1~11/1) |
| 커튼콜 위크 (촬영 허용) | 10/13 ~ 10/18 |

**할인 9종 / 웰컴키트 4종 / 재관람 카드 3단계** — `show-info.json`에 기록

### 2.2 미확보

| 항목 | 상태 | 사유 |
| --- | --- | --- |
| 2차 오픈분 캐스팅 스케줄 (11/2 ~ 12/13) | ❌ | **아직 미공개.** 티켓 오픈 시 재수집 필요 |
| 배역 공식 영문 표기 | ❌ | 공식 자료에 영문 배역명 없음. 현재 키는 임의 로마자 |

### 2.3 수집 경로 기록

막혔던 경로:

- `ticketlink.co.kr/product/65293` · 모바일 · `/schedule` — 전부 제목만 반환 (SPA)
- `x.com/_LIVECONNECTION` 비로그인 — **HTTP 402**
- 검색엔진 색인 (플레이DB / KOPIS / 뮤연 / 캐스팅필터) — 스케줄 데이터 없음
- gallery-dl `--cookies-from-browser chrome` — **DPAPI 복호화 실패** (Chrome 152 App-Bound Encryption)

성공한 경로:

- Chrome 확장 `Get cookies.txt LOCALLY` 로 `cookies.txt` 내보내기 → gallery-dl 인증
- `from:_LIVECONNECTION 이끼숲` 검색으로 트윗 25건 / 이미지 37장 수집
- 공식 안내 이미지 판독으로 위 정보 전량 확보

**핵심 자료는 전부 X 이미지에만 존재했다.** 웹 검색으로는 캐스팅 라인업까지가 한계였음.

---

## 3. 데이터 모델

### 3.1 `schedule.json` — 회차별 캐스팅

```jsonc
[
  {
    "date": "2026-10-01",   // YYYY-MM-DD
    "time": "19:30",        // HH:MM
    "marco":  "송유택",
    "soma":   "강연정",
    "yuo":    "정욱진",
    "uiju":   "정다예",
    "eunhui": "김청아"
  }
]
```

- 하루 2회차면 같은 `date`로 두 개 항목 (`time`으로 구분)
- 배역 키는 `show-info.json`의 `roles[].key`와 정확히 일치해야 함
- 앱이 `fetch('./schedule.json')`으로 읽음 (베이스 기준 `index.html:2262`)

### 3.2 `events.json` — 특전 기간

```jsonc
[
  {
    "name": "첫공 무대인사",
    "startDate": "2026-10-01",
    "endDate": "2026-10-04",
    "color": "#4DDB94"      // 캘린더 띠 색상
  }
]
```

- 앱이 `fetch('./events.json')`으로 읽음 (`index.html:2286`)

### 3.3 `show-info.json` — 작품 메타 (신규)

베이스에 없던 파일. 배역·배우·공연정보를 한 곳에 모아 두어
`index.html` 하드코딩을 생성/검증할 때 단일 출처로 쓴다. 이미 작성 완료.

---

## 4. 베이스 코드 개조 지점

`index.html`(195KB 단일 파일)에 배역 키가 **13곳**에 하드코딩되어 있다.
아래 줄 번호는 베이스 원본 기준.

| # | 위치 | 내용 | 작업 |
| --- | --- | --- | --- |
| 1 | `:6` | `<title>⚔️ 비더슈탄트 관극 다이어리` | 제목·이모지 교체 |
| 2 | `:338–348` | 캘린더 배역 탭 버튼 6개 | 5개로 교체 |
| 3 | `:437–447` | 페어 필터 배역 탭 6개 | 5개로 교체 |
| 4 | `:582–620` | 기록 폼 `<select id="rec-*">` 6개 + 배우 `<option>` | 5개로 교체, 배우 15명 반영 |
| 5 | `:1348–1370` | `DISCOUNT_COUPON_MAP` / `DISCOUNT_LABEL` | 이끼숲 할인 9종으로 교체 (§4.3) |
| 6 | `:1452–1459` | `const ROLES` 배열 | 5배역으로 교체 |
| 7 | `:1547–1550` | `FLOOR_ROWS` (1층 A–N / 2층 B–G) | 백암아트홀 구성으로 교체 (§4.4) |
| 8 | `:1806–1808` | 필수 입력 검증 (`magnus`, `abel`) | 주역 2개 키로 교체 |
| 9 | `:1835–1843` | `record` 객체 배역 필드 | 5키로 교체 |
| 10 | `:1967` | 목록 요약 `[r.magnus, r.abel]` | 주역 2개 키로 교체 |
| 11 | `:2484` | `calFilter` 초기값 6키 | 5키로 교체 |
| 12 | `:2852` | `SHORT` 약칭 맵 | 5배역 약칭으로 교체 |
| 13 | `:3300` | 중복 기록 경고 문자열 | 주역 2개 키로 교체 |

부가 파일:

| 파일 | 작업 |
| --- | --- |
| `manifest.json` | 앱 이름·테마색 |
| `sw.js` | 캐시 버전 갱신 (배포 시 필수) |
| `guide.html` | 사용 안내 문구·스크린샷 |
| `icon-192.png` / `icon-512.png` | 아이콘 교체 |

### 4.1 배역 6→5 축소 시 주의

`ROLES` 배열만 줄이면 JS 로직(`ROLES.forEach`, `ROLES.map`)은 자동으로 따라오지만,
**HTML에 정적으로 박힌 #2·#3·#4는 수동으로 지워야 한다.**
`claire` 같은 잔여 키가 남으면 `document.getElementById('rec-claire')`가 `null`이 되어
`saveRecord()`에서 터진다. 개조 후 전체 검색으로 구 배역 키 6종
(`magnus` `abel` `fredrick` `hagen` `jasper` `claire`) 잔존 여부를 반드시 확인.

### 4.2 주역 지정

**결정 (2026-09-05): 필수 배역을 두지 않는다.**

베이스는 `magnus`/`abel` 두 배역을 필수 입력으로 강제하고, 미입력 시 저장을 막는다.
이끼숲에서는 이 강제를 **제거**한다. 배역 5개 모두 선택 입력으로 두고,
날짜·시간만 필수로 남긴다.

영향받는 코드:

| 위치 | 현재 | 변경 |
| --- | --- | --- |
| `:1806–1808` | `magnus`/`abel` 값 검사 후 `showToast` 차단 | 날짜·시간만 검사 |
| `:1835–1843` | `magnus`, `abel`은 무조건 값이 있다고 가정 | 5개 모두 `|| ''` 처리 |
| `:1967` | `[r.magnus, r.abel]`로 목록 요약 | 값이 있는 배역 앞 2개로 요약 |
| `:3300` | 중복 경고에 `(${r.magnus}·${r.abel})` | 값이 있는 배역으로 구성 |
| `:585, 592` | 라벨에 `*` (필수 표시) | `*` 제거 |

> 주의: `:1967`과 `:3300`은 두 배역이 항상 채워져 있다고 가정한다.
> 전부 비워두면 `' · '` 같은 빈 문자열이 나오므로 폴백 문구가 필요하다.
> `TEST_PLAN.md` R6을 "미입력 저장이 **허용**되는지"로 바꿔 검증한다.

### 4.3 할인·쿠폰 체계

**베이스와 이끼숲의 쿠폰 구성이 정확히 일치한다.** 웰컴 키트 4종이
베이스의 쿠폰 키 4개와 1:1로 대응하므로 구조 변경 없이 라벨만 맞추면 된다.

| 웰컴 키트 (이끼숲) | 베이스 쿠폰 키 |
| --- | --- |
| 40% 할인권 1매 | `discount40` |
| 50% 할인권 1매 | `discount50` |
| 할인 증빙 프리패스권 1매 | `eungBingPass` |
| 더블 적립권 1매 | `doubleStamp` |

`DISCOUNT_LABEL`은 이끼숲 할인 9종으로 교체한다.

| 할인명 | 할인율 | 매수제한 |
| --- | --- | --- |
| 프리뷰 할인 | 50% | 1인 4매 |
| 1차 조기예매 할인 | 35% | 1인 4매 |
| 재관람 할인 | 30% | 1인 1매 |
| 청소년 할인(S석) | 30% | 1인 1매 |
| 직장인 할인 | 20% | 1인 1매 |
| 도서 소지 할인 | 15% | 1인 1매 |
| SNS 친구 할인 | 10% | 1인 1매 |
| 장애인 / 국가유공자 할인 | 50% | 본인 한정 |

**도장판**: 재관람 카드가 4회·6회·8회 3단계이므로 `totalSlots`를 **8**로 두고
4·6·8에 보상을 건다. (베이스 기본값은 7)

**초과분 이월 규칙 (확정, 2026-09-05)**

공식 안내 원문:

> 두 개의 도장을 서로 다른 판에 나누어 받을 수 없습니다. (분할 적립 불가)
> 8회 적립 시, 새로운 카드에 이어서 적립 가능합니다.

실제 운영 기준은 다음과 같다.

| 상황 | 처리 |
| --- | --- |
| 판에 잔여 2칸 이상 + 더블 적립 | 같은 판에 2개 적립 |
| 판에 잔여 1칸 + 더블 적립 | 1개로 현재 판 완성 → **초과 1개는 새 판을 만들어 적립** |
| 판이 가득 참 + 신규 적립 | **새 판을 만들어** 적립 |

**핵심 제약: 초과분은 반드시 새로 생성한 판으로 간다.**
이미 존재하는 미완성 판에 초과분을 얹을 수 없다.
*"분할 적립 불가"* 는 보유 중인 여러 판에 도장을 임의로 나눠 받는 것을 막는 문구이지,
판이 완성될 때의 이월을 막는 것이 아니다.

베이스의 `STAMP OVERFLOW CONFIRM` 패널(`:878`)이 이월 자체는 이미 처리한다.
확인할 지점은 **이월 대상이 새로 만든 판인지, 기존 미완성 판인지**다.
기존 판을 재사용한다면 그 부분만 고친다 → `TEST_PLAN.md` S1·S9.

공식 문구 *"여러 장의 카드 합산이 불가합니다"* 가 이 제약의 근거다.

### 4.3.1 재관람 카드 적립 규칙

재관람 카드 안내 2번째 이미지에서 확인된 제약. **베이스 동작과 충돌하는 항목이 있다.**

| 규칙 | 앱 영향 |
| --- | --- |
| 카드 발급은 유료 공연 1회차부터 | 도장판 생성 시점 |
| 한 회차당 유료 티켓 1매만 적립, **동일 회차 중복 적립 불가** | 같은 회차 기록 2건이 각각 적립되면 안 됨 |
| **지난 회차 적립 불가 (소급 적립 없음)** | ⚠️ **베이스의 "기존 기록 소급 스캔"(`TEST_SCENARIOS.md` G)과 충돌** |
| **여러 장의 카드 합산 불가** | 초과분 이월이 새 판으로 가야 하는 근거 |
| 2회 공연 시 해당 회차 MD부스 운영시간에만 발급·적립 | 기록용 앱에서는 제약 없음 |
| 분실·훼손 재발급 불가, 타인 양도·미지참 시 적립 불가 | 안내 문구로만 |

**할인권 유효기간**

| 항목 | 값 |
| --- | --- |
| 사용·적용 기간 | **~ 2026-12-11** (공연 종료 12/13보다 이틀 빠름) |
| 매진 시 | 사용 불가 |
| 예매 시 | '재관람 혜택 40% / 50% 할인권 소지자 할인' 권종 선택 |
| 당일 | 매표소에 실물 제시. 미지참 시 정가 기준 차액 지불 |

> 공연 마지막 이틀(12/12~12/13)은 재관람 할인권을 쓸 수 없다.
> 잔여 할인권 만료 안내가 필요한 지점.

**폴라로이드(8회 보상)**

- 당일 회차 캐스트 중에서만 지정 가능
- 수령 일자는 라이브커넥션 SNS로 추후 공지 (X `@_LIVECONNECTION` / 인스타그램 `lc.musical_`)
- 입고 전까지는 **교환권**으로 증정 → 보상 상태에 "교환권 수령 / 실물 수령" 구분이 필요할 수 있음

> **소급 적립 충돌 처리**: 베이스는 기존 기록을 훑어 도장을 소급 적립하는 기능이 있다.
> 이끼숲 규칙상 현장에서는 불가능하므로, 이 기능을 그대로 두면 앱 숫자와 실제 카드가 어긋난다.
> 제거하거나 "실제 카드와 다를 수 있음" 경고를 붙여야 한다 → §7 열린 질문.

### 4.4 좌석 구성

백암아트홀 SEATING PLAN 판독 결과:

```js
const FLOOR_ROWS = {
  '1층': ['A','B','C','D','E','F','G','H','I','J','K','L','M'],  // R석
  '2층': ['N','O','P','Q','R']                                    // S석
};
```

- 1층 R석 13열, 열별 좌석번호 최대 1~26번 (열마다 시작·끝 번호가 다름)
- 2층 S석 5열, 좌석번호 1~22번
- 1층 M열 우측에 휠체어석 5석 — 예매는 티켓링크 고객센터(1588-7890) 전용

> 베이스는 1층에 E열이 없고 2층이 B~G였다. 열 구성이 완전히 다르므로 통째로 교체.

### 4.5 현장 할인 변경 규칙 (비공식)

> **출처: 사용자 제공.** 공식 공지에는 없는 현장 운영 관행이므로 별도 표시한다.

예매할 때 적용한 할인의 증빙(실물 할인권 등)을 관람 당일 지참하지 못한 경우,
**정가 기준 차액을 전액 지불하는 대신, 현장에서 증빙 가능한 더 낮은 할인율로 변경하고
그 차액만 지불**할 수 있다.

예: 50% 할인권으로 예매 → 당일 실물 할인권 미지참 → 재관람 할인(30%)으로 변경 →
50%와 30%의 차액만 지불 (정가 차액이 아님).

공식 DISCOUNT 이미지에는 *"미지참 시 정가 기준 차액 지불"* 로만 적혀 있어,
이 완화 규칙은 문서화되어 있지 않다.

**앱 설계 영향**

베이스는 `discountType` 하나와 `paidDiff`(차액지불) 불리언만 갖는다.
이 규칙을 제대로 기록하려면 **예매 시 할인**과 **실제 적용된 할인**을 분리해야 한다.

```jsonc
{
  "discountType":       "50% 실물 할인권",  // 예매 시
  "appliedDiscountType": "재관람 할인",       // 현장에서 실제 적용 (신규)
  "paidDiff": true,
  "diffAmount": 15400                        // 실제 지불한 차액 (신규, 선택)
}
```

이렇게 두면 통계에서 *"할인권 없이 넘어간 회차"* 와 *"실제 지출"* 을 정확히 집계할 수 있다.
다만 입력 폼이 복잡해지므로, **간소화 안**(기존 `paidDiff`만 쓰고 차액 금액만 기록)과
둘 중 선택이 필요하다 → §7 열린 질문에 추가.

---

## 5. 자료 수집 파이프라인

구축 완료 — [`d:\workspace\x-collector`](../x-collector/)

```
collect.ps1 ──> gallery-dl ──> data/raw/<계정>/<트윗ID>_<n>.jpg
                                             + .json (메타데이터)
                     │
                     └──> build_dataset.py ──> data/dataset.json  (구조화 데이터)
                                          └──> data/review.md     (이미지 판독 체크리스트)
```

| 구성요소 | 상태 |
| --- | --- |
| Python 3.12.10 | 설치 완료 |
| gallery-dl 1.32.11 | 설치 완료 |
| `collect.ps1` / `build_dataset.py` / `gallery-dl.conf` | 작성·문법 검증 완료 |
| `config/cookies.txt` | 설정 완료 (Chrome 확장 내보내기) |
| 수집 실적 | 트윗 25건 / 이미지 37장 |

수집 대상 명령:

```powershell
cd d:\workspace\x-collector
.\collect.ps1 -Url "https://x.com/_LIVECONNECTION/media" -Limit 300
```

수집 후 캐스팅표 이미지를 판독해 `schedule.json` / `events.json`으로 옮긴다.

### 5.1 쿠키 처리 (해결됨)

X는 비로그인 접근을 전면 차단한다 — `x.com/_LIVECONNECTION` 요청은 **HTTP 402**.

이 PC의 Chrome 152는 **App-Bound Encryption**을 쓰기 때문에
gallery-dl의 `--cookies-from-browser chrome`이 실패한다 (`Failed to decrypt cookie (DPAPI)`).
Chrome을 종료한 뒤에도 동일. 따라서 확장 프로그램을 통한 `cookies.txt` 내보내기를 사용했다.

- 확장: [`Get cookies.txt LOCALLY`](https://github.com/kairi003/Get-cookies.txt-LOCALLY) (오픈소스, 외부 전송 없음)
- 결과: `auth_token` · `ct0` · `twid` 포함 13개 쿠키, Netscape 형식

> `config/cookies.txt`는 **계정 접속 권한 그 자체**다. 커밋·공유 금지.
> 로그아웃 시 무효화되므로 수집 실패 시 재내보내기.

### 5.2 2차 오픈분 재수집

11/2~12/13 캐스팅이 공개되면 아래만 다시 돌리면 된다.

```powershell
cd d:\workspace\x-collector
.\collect.ps1 -Query "from:_LIVECONNECTION 이끼숲" -Limit 200
```

`archive.sqlite3`가 기존 수집분을 기억하므로 **새 게시물만** 내려받는다.

---

## 6. 작업 순서

| 단계 | 내용 | 상태 |
| --- | --- | --- |
| 1 | 수집 파이프라인 구축 | ✅ 완료 |
| 2 | `cookies.txt` 설정 | ✅ 완료 |
| 3 | @_LIVECONNECTION 수집 (25건 / 37장) | ✅ 완료 |
| 4 | 캐스팅표 판독 → `schedule.json` (39회차) | ✅ 완료 |
| 5 | 특전 판독 → `events.json` (3건) | ✅ 완료 |
| 6 | 공연정보·할인·좌석 판독 → `show-info.json` | ✅ 완료 |
| 7 | 데이터 검증 (`scripts/validate.py`) | ✅ 통과 |
| 8 | 설계서 검토·확정 | ⏳ **사용자 확인 대기** |
| 9 | `index.html` 13개 지점 개조 | 8 이후 |
| 10 | 로컬 검증 (`TEST_SCENARIOS.md` 기준) | 9 |
| 11 | GitHub Pages 배포 | 10 |
| 12 | 2차 오픈 시 스케줄 재수집·병합 (§5.2) | 티켓 오픈 후 |

**현재 임계 경로는 8단계 — 설계서 확정.** 데이터는 전부 준비됐다.

### 6.1 데이터 검증 결과

`scripts/validate.py` 실행 결과:

```
공연 회차 39건 / 이벤트 3건
공연일 28일
월요일 공연: ['2026-10-05']
  마르코  송유택 13 · 현석준 13 · 홍승안 13
  소마   강연정 13 · 강혜인 13 · 전혜주 13
  유오   이주순 13 · 이한솔 13 · 정욱진 13
  의주   오유민 13 · 이혜진 13 · 정다예 13
  은희   김청아 13 · 윤지우 13 · 한비 13

검증 통과 — 오류 없음
```

검사 항목: 날짜 형식 · 공연기간 범위 · 회차 중복 · 요일별 공연시간(공휴일 포함) ·
배역 누락 · 캐스팅 목록 대조 · 미지 필드.

**배우 15명 전원이 정확히 13회씩 배정된다.** 전사 오류가 있었다면 이 대칭이 깨졌을 것이므로,
판독 정확도의 강한 방증이다. 월요일 공연이 10/5 하나뿐인 점도 공식 안내와 일치.

---

## 7. 열린 질문

1. **저장소 이름** — `sw.js`·`manifest.json`에 `/widerstand2026` 경로가 하드코딩돼 있다.
   새 저장소 이름이 확정돼야 교체 가능 (§10)
2. **소급 적립 기능 처리** — 베이스의 "기존 기록 소급 스캔"을 어떻게 할 것인가? (§4.3.1)
   현장 규칙상 지난 회차 적립은 불가능하다.
   - (A) 기능 제거 — 앱 숫자와 실제 카드가 항상 일치
   - (B) 유지하되 "실제 카드와 다를 수 있음" 경고 표시
   - (C) 유지 — 개인 기록용이므로 현장 규칙과 무관하게 관리
3. **할인 변경 기록 방식** — §4.5의 현장 할인 변경을 어떻게 기록할 것인가?
   - (A) 정밀안: `appliedDiscountType` + `diffAmount` 필드 추가
   - (B) 간소안: 기존 `paidDiff` 유지 + 차액 금액만 기록
4. **테마 색상** — 공식 이미지 키컬러는 딥틸(`#1A6B72` 계열) + 페일민트 배경 + 코럴 포인트.
   이 팔레트로 잡아도 되는가?
5. **앱 이모지·아이콘** — 베이스는 ⚔️. 이끼숲은 🌿 계열이 자연스러운데 확정 필요 (§10)
6. **배역 영문 키** — 임의 로마자 표기. 공식 표기가 따로 있으면 교체 필요

**해소된 항목**

- ~~주역 2개 배역~~ → **필수 배역 없음으로 결정** (2026-09-05). 날짜·시간만 필수 (§4.2)
- ~~할인 정책~~ → 공식 DISCOUNT 이미지로 9종 전량 확보 (§4.3)
- ~~캐스팅 매칭 검증~~ → 공식 캐스팅 공개 게시물로 5배역 15명 대조 완료
- ~~도장판 기본값~~ → 재관람 카드 기준 `totalSlots` 8, 보상 4·6·8회로 확정
- ~~초과분 이월 처리~~ → **초과분은 다음 판으로 이월하되 반드시 새 판을 생성** (2026-09-05).
  공식 문구 *"여러 장의 카드 합산 불가"* 가 근거 (§4.3)

---

## 8. 리스크

| 리스크 | 영향 | 대응 |
| --- | --- | --- |
| 캐스팅 스케줄이 이미지로만 존재 | 판독 오류 시 잘못된 데이터 | 판독 후 원본 이미지와 대조 검수 |
| 공연 중 캐스팅 변경 공지 | 데이터 노후화 | 수집 명령 재실행 (`archive.sqlite3`가 증분 처리) |
| 쿠키 만료 | 수집 중단 | 재로그인 후 `cookies.txt` 재내보내기 |
| X 이용약관 | 계정 제한 가능성 | 개인적 범위 유지, `-Limit` 보수적 설정 |
| 배역 6→5 축소 누락 | 런타임 오류 | §4.1 전수 검색, `TEST_PLAN.md` E1 |
| 좌석 판독 오류 | 히트맵 왜곡 | 공표 객석수 417석과 대조 완료 (§4.4) |

---

## 9. 기능 확장 제안

수집 과정에서 확보한 데이터를 활용하면 베이스에 없던 기능을 싸게 붙일 수 있다.
**데이터 보유** 열이 ✅면 추가 수집 없이 바로 구현 가능하다는 뜻이다.

### 9.1 수집 데이터 활용 (구현 비용 낮음)

| # | 기능 | 근거 데이터 | 데이터 보유 | 비용 |
| --- | --- | --- | --- | --- |
| 1 | **이벤트 → X 게시물 이동** | `events[].url` | ✅ | 낮음 |
| 2 | **배역별 캐릭터 포스터** — 배우 선택 시 해당 배우 포스터 표시 | 수집한 캐릭터 포스터 15장 | ✅ | 낮음 |
| 3 | **할인 안내 시트** — 할인 9종 조건·매수제한 조회 | `show-info.json > discounts` | ✅ | 낮음 |
| 4 | **공연 정보 카드** — 러닝타임·연령·가격·MD부스 운영시간 | `show-info.json` | ✅ | 낮음 |
| 5 | **쿠폰 사용 규칙 안내** — 웰컴키트 4종의 사용 조건 | `show-info.json > welcomeKit.usageRules` | ✅ | 낮음 |

> #1은 사용자 제안. `events.json`에 `url`·`description`을 이미 넣어두었다.
> #2는 이미지 15장이 `x-collector/data/raw/`에 있으므로 앱으로 복사만 하면 된다.

### 9.2 관극 다이어리 특화

| # | 기능 | 설명 | 근거 데이터 | 비용 |
| --- | --- | --- | --- | --- |
| 6 | **재관람 카드 진행률** | 4·6·8회 마일스톤까지 남은 횟수를 홈에 표시 | `rewatchCard.milestones` ✅ | 중간 |
| 7 | **미관람 캐스팅 조합** | 39회차 조합 중 안 본 것 추천. 베이스 통계의 조합 표를 역으로 활용 | `schedule.json` ✅ | 중간 |
| 8 | **잔여 회차 카운터** | 공연 종료까지 남은 회차·일수 | `schedule.json` ✅ | 낮음 |

> **제외 결정 (2026-09-05)**: 캐릭터 명대사 · 아직 못 본 배우 계산 · 특이 회차 배지는 사용자 판단으로 제외.
> `show-info.json`의 `roles[].quote`는 자료로만 남겨둔다.

### 9.3 지출·좌석

| # | 기능 | 설명 | 비용 |
| --- | --- | --- | --- |
| 9 | **절감액 대시보드** | 정가(R 77,000 / S 66,000) 대비 실제 지출과 누적 절감액. 할인 9종 데이터로 계산 가능 | 중간 |
| 10 | **좌석 메모·평점** | 앉았던 좌석에 시야 메모를 남기고 히트맵에서 조회. 417석 좌표를 이미 확보 | 중간 |
| 11 | **비판매석 표시** | 회색 14석·휠체어석 5석을 히트맵에 구분 표시 | 낮음 |

### 9.4 운영

| # | 기능 | 설명 | 비용 |
| --- | --- | --- | --- |
| 12 | **2차 오픈 안내** | 11/2~12/13 구간을 "스케줄 미공개"로 명시해 빈 캘린더 오해 방지 | 낮음 |
| 13 | **데이터 갱신 배너** | `schedule.json`에 버전·갱신일을 넣고, 2차 병합 시 사용자에게 알림 | 중간 |
| 14 | **특전 일정 추가 반영** | 공연 개막 후 발표되는 포토카드·무대인사 등을 `events.json`에 증분 반영 | 중간 |
| 15 | **캐스팅 변경 이력** | 제작사가 변경 공지 시 이전 스케줄과 diff 표시. `archive.sqlite3` 증분 수집과 연동 | 높음 |

### 9.5 추천 착수 순서

데이터가 이미 있고 체감 가치가 큰 것부터.

1. **#1 이벤트 링크** · **#12 2차 오픈 안내** — 각각 수십 줄
2. **#4 공연 정보 카드** · **#3 할인 안내 시트** · **#5 쿠폰 사용 규칙** — 정적 렌더링
3. **#8 잔여 회차** · **#6 재관람 카드 진행률** — 홈·통계 확장
4. **#2 캐릭터 포스터** — 이미지 15장 복사 + 매핑
5. 나머지는 앱 개조가 안정된 뒤

> 주의: #9·#10·#15는 기록 스키마를 건드린다.
> 스키마를 바꾸면 `TEST_PLAN.md`의 R·C 시나리오를 다시 돌려야 한다.

---

## 10. 비더슈탄트 잔재 전수 조사

2026-09-05 실시. 단어경계(`\b`)를 적용해 오탐을 제거한 결과다.

> **오탐 주의**: `label`에 `abel`이, `이한솔`이 양 작품 캐스팅에 모두 존재한다.
> 단순 문자열 검색은 실제보다 많은 건수를 보고하므로 반드시 단어경계를 쓸 것.

### 10.1 파일별 집계 (앱 파일만)

| 파일 | 배역키 | 배역한글 | 작품명 | 배우명 | 이모지 |
| --- | --- | --- | --- | --- | --- |
| `index.html` | **68** | **30** | 4 | **34** | 4 |
| `manifest.json` | — | — | **5** | — | — |
| `guide.html` | — | — | **5** | — | 2 |
| `sw.js` | — | — | **2** | — | — |

### 10.2 index.html — 작품명·이모지 (6개 지점)

| 줄 | 내용 | 변경 |
| --- | --- | --- |
| `6` | `<title>⚔️ 비더슈탄트 관극 다이어리</title>` | 🌿 이끼숲 관극 다이어리 |
| `11` | `<meta apple-mobile-web-app-title content="비더슈탄트">` | 이끼숲 |
| `227` | `.next-show-card::before{content:'⚔️'}` | 이모지 교체 |
| `296` | `<div class="page-title">⚔️ 비더슈탄트</div>` | 이모지·제목 교체 |
| `1229` | 백업 파일명 `` `widerstand_backup_${...}` `` | `ikkisup_backup_` |
| `2380` | 빈 상태 안내 아이콘 `⚔️` | 이모지 교체 |

> `1229`는 **사용자 백업 파일명**이라 놓치기 쉽다. 기존 백업 파일과 이름이 섞이지 않도록 반드시 교체.

### 10.3 manifest.json — 전면 교체

```jsonc
{
  "name": "비더슈탄트 관극 다이어리",        // → 이끼숲 관극 다이어리
  "short_name": "비더슈탄트",                // → 이끼숲
  "description": "뮤지컬 비더슈탄트 2026 ...", // → 뮤지컬 이끼숲 2026 관람 기록 앱
  "start_url": "/widerstand2026/",           // → /<새 저장소명>/
  "scope": "/widerstand2026/",               // → /<새 저장소명>/
  "background_color": "#F8F5F1",             // → 이끼숲 팔레트
  "theme_color": "#9B7B5A"                   // → 이끼숲 팔레트 (현재는 베이지/브라운)
}
```

### 10.4 sw.js — 경로·캐시 버전

```js
const CACHE_NAME = 'widerstand2026-v10';  // → '<새 저장소명>-v1'
const BASE = '/widerstand2026';            // → '/<새 저장소명>'
```

> **가장 위험한 항목.** `BASE`가 틀리면 PWA가 엉뚱한 경로를 프리캐시해 오프라인에서 깨진다.
> `CACHE_NAME`을 갱신하지 않으면 기존 사용자에게 옛 파일이 계속 서빙된다.
> 저장소 이름이 확정돼야 고칠 수 있다 → §7 열린 질문 1.

### 10.5 guide.html

| 줄 | 내용 | 변경 |
| --- | --- | --- |
| `6` | `<title>비더슈탄트 관극 다이어리 — 사용 가이드</title>` | 제목 |
| `36` | `<h1>⚔️ 비더슈탄트 관극 다이어리</h1>` | 제목·이모지 |
| `37` | `<p>비더슈탄트 2026 관람 기록 앱</p>` | 문구 |
| `98` | `musical-otk.github.io/widerstand2026/` | 새 배포 URL |
| `103` | `⚔️ 비더슈탄트 2026 · **링크아트센터드림 드림 1관**` | 이끼숲 2026 · **백암아트홀** |

> `103`에는 **옛 극장명**이 남아 있다. 작품명만 바꾸고 지나치기 쉬운 지점.

### 10.6 아이콘

`icon-192.png`(12.2KB) · `icon-512.png`(86.7KB) 모두 **비더슈탄트 포스터 이미지**다.
확인 결과 펜싱검 모티프에 `비더슈탄트 / WIDERSTAND`, 하단에
`2026.07.23 - 10.11 링크아트센터드림 드림1관`까지 박혀 있다. 통째로 교체 대상.

대체 후보: 수집한 이끼숲 공식 포스터
`x-collector/data/raw/_LIVECONNECTION/2089864959727682046_1.jpg`

> 아이콘 교체 후 `sw.js`의 `CACHE_NAME`을 반드시 올릴 것.
> 안 그러면 기존 사용자 홈화면에 옛 아이콘이 그대로 남는다.

### 10.7 문서 파일

`TEST_SCENARIOS.md`는 베이스의 버그 이력(B1~B8)이라 **작품명·배역명을 그대로 두는 편이 낫다.**
이끼숲 기준 시나리오는 `TEST_PLAN.md`에 새로 작성했고, 베이스 문서는 회귀 검증용 참고 자료로 보존한다.

### 10.8 검증 명령

개조 후 아래가 **0건**이어야 한다 (`TEST_PLAN.md` E1).

```powershell
Get-ChildItem d:\workspace\ikkisup2026 -Include index.html,manifest.json,guide.html,sw.js -Recurse |
  Select-String -Pattern '\b(magnus|abel|fredrick|hagen|jasper|claire)\b|비더슈탄트|[Ww]iderstand|⚔|링크아트센터'
```
