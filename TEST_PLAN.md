# 셀프 검사 체계 — 뮤지컬 〈이끼숲〉 관극 다이어리

작성일 2026-09-05 · 베이스 `TEST_SCENARIOS.md`(쿠폰 시나리오 A~L, 버그 B1~B8)를 승계하고 확장

---

## 1. 왜 3계층인가

이 앱의 버그는 성격이 셋으로 갈린다. 각각 잡는 방법이 달라서 계층을 나눈다.

| 계층 | 대상 | 방식 | 실행 시점 |
| --- | --- | --- | --- |
| **L1 데이터** | `schedule.json` `events.json` `seating.json` `show-info.json` | `scripts/validate.py` — 자동 | 데이터 수정 시마다 |
| **L2 로직** | 기록·도장·쿠폰의 상태 전이 | `selftest.html` 하네스 — 자동 | 코드 수정 시마다 |
| **L3 UI** | 렌더링·필터·레이아웃 | 시나리오 수동 확인 | 배포 전 |

베이스의 버그 8건을 되짚어보면 **B1·B7·B8은 L2에서, B6은 L3에서** 잡혔을 것들이다.
L1은 이번에 새로 도입한 계층으로, 판독 데이터가 원본과 어긋나는 사고를 막는다.

### L1 현황 (이미 동작 중)

```
공연 회차 39건 / 이벤트 3건
좌석 417석 1층 309(R석) · 2층 108(S석)
  비판매석 14석 · 휠체어석 5석 · 실판매 398석
공연일 28일
월요일 공연: ['2026-10-05']
  마르코  송유택 13 · 현석준 13 · 홍승안 13
  ...
검증 통과 — 오류 없음
```

---

## 2. L2 하네스 설계 (`selftest.html`)

앱이 단일 HTML + localStorage이므로 외부 테스트 도구 없이 자체 검사가 가능하다.

```
selftest.html
  └─ <iframe src="index.html">        ← 실제 앱을 그대로 로드
       ├─ 시나리오마다 localStorage 초기화
       ├─ iframe 내부 전역 함수 직접 호출 (saveRecord, deleteRecord, …)
       └─ 종료 상태를 assert
```

**핵심 원칙**

1. **격리** — 시나리오마다 `localStorage.clear()` 후 시작. 순서 의존성을 없앤다.
2. **상태 비교** — UI가 아니라 `getRecords()` / `getCoupons()` / `getStampBoards()`의
   반환값을 비교한다. 렌더링 변경에 흔들리지 않는다.
3. **역방향 검증** — "추가 → 삭제 후 원상복구"를 항상 짝으로 확인.
   베이스 버그 B7·B8이 전부 이 패턴에서 나왔다.
4. **결과 표시** — 통과/실패 목록을 페이지에 렌더링. 실패 시 기대값·실제값을 나란히.

배포 대상이 아니므로 `selftest.html`은 GitHub Pages에 올리되 링크는 노출하지 않는다.

---

## 3. 시나리오 목록

우선순위 **P1**=데이터 손실·계산 오류 / **P2**=기능 오작동 / **P3**=표시 문제

### R. 기록 생명주기

| ID | 전제 | 조작 | 기대 결과 | P |
| --- | --- | --- | --- | --- |
| R1 | 빈 상태 | 기록 추가 → 도장판 선택 → **기록 삭제** | 도장도 함께 회수. 도장판 카운트 원복 | **P1** |
| R2 | R1 상태 | 기록 편집으로 **도장판 변경** | 이전 판 −1, 새 판 +1 | P1 |
| R3 | R1 상태 | 기록 편집으로 **도장 해제** | 적립 취소, 판 카운트 −1 | P1 |
| R4 | 할인권 사용 기록 있음 | 기록 삭제 | 사용했던 할인권 복원 | P1 |
| R5 | 같은 날짜·시간 기록 존재 | 동일 회차 재저장 | 중복 경고 노출 | P2 |
| R6 | — | 배역 5개 **전부 미입력**으로 저장 | 저장 **허용** (필수 배역 없음). 목록·중복경고에 빈 문자열 대신 폴백 문구 | P2 |
| R7 | 미확정(예매 전) 기록 | 통계 화면 진입 | 관람 횟수에서 제외 | P2 |
| R8 | 도장 선택된 기록 | 날짜만 변경 후 저장 | 도장 연동 유지, 중복 적립 없음 | P1 |

### S. 도장판

| ID | 전제 | 조작 | 기대 결과 | P |
| --- | --- | --- | --- | --- |
> **초과분 이월 규칙 (확정)**
> 판이 완성되면 초과분은 다음 판으로 이월된다. 단 **이월 대상은 반드시 새로 생성한 판**이며,
> 이미 보유 중인 미완성 판에는 얹을 수 없다.
> *"분할 적립 불가"* 는 보유한 여러 판에 도장을 임의로 나눠 받는 것을 막는 문구다.

| ID | 전제 | 조작 | 기대 결과 | P |
| --- | --- | --- | --- | --- |
| S1 | 8칸 판에 **7칸** 적립됨 | **더블 적립**으로 기록 저장 | 1개로 현재 판 완성(8/8) + **초과 1개는 새로 만든 판**에 적립 | **P1** |
| S2 | 8칸 판이 가득 참 | 신규 기록 저장 | **새 판 생성** 후 적립 | P1 |
| S3 | 8칸 판 진행 중 | 4·6·8칸 도달 | 각 시점에 40%·50%·폴라로이드 보상 안내 | P2 |
| S4 | 일시중지된 판 존재 | 기록 폼 열기 | 중지된 판은 선택 목록에서 제외 | P2 |
| S5 | 기록이 연결된 판 | 도장판 삭제 | 기록의 `stampBoardId` 정리, 고아 참조 없음 | **P1** |
| S6 | S1 처리 직후 (판 2개) | 그 기록을 **삭제** | 두 판 모두 롤백. 이월로 생성된 판이 비면 정리 | **P1** |
| S7 | 판 잔여 1칸 | 더블 적립 저장 → **편집으로 더블 해제** | 이월로 생성된 판 취소, 원래 판 8/8 아닌 상태로 복귀 | P1 |
| S8 | 잔여 2칸 이상 | 더블 적립 저장 | **같은 판에** 2개 적립 (이월 없음) | P1 |
| S9 | **미완성 판이 이미 2개** 존재, 그중 하나 잔여 1칸 | 그 판에 더블 적립 | 초과분이 **기존 다른 미완성 판으로 가면 실패**. 새 판을 만들어야 통과 | **P1** |
| S10 | 같은 날짜·시간 회차 | 기록 2건 저장 후 각각 도장 선택 | **동일 회차 중복 적립 불가** — 도장 1개만 적립되거나 경고 | P2 |
| S11 | 과거 날짜 기록 추가 | 도장 선택 | **소급 적립 불가** 규칙 반영 여부 (베이스 소급 스캔과 충돌, §DESIGN 4.3.1) | P2 |
| S12 | 12/11 이후 회차 | 재관람 할인권 사용 시도 | 할인권 **유효기간 만료** 안내 | P2 |

> S9가 이번 규칙의 핵심 검사다. 베이스는 이월 자체는 처리하지만
> **이월 대상을 새로 만드는지 기존 판을 재사용하는지**가 확인되지 않았다.
> S1·S6·S7은 베이스 `TEST_SCENARIOS.md` H·I·J의 확장이며, 베이스는 7칸·이끼숲은 8칸이라 경계값이 다르다.

### C. 쿠폰·할인

| ID | 전제 | 조작 | 기대 결과 | P |
| --- | --- | --- | --- | --- |
| C1 | 50% 할인권 0개 | 50% 할인권으로 저장 | 저장 차단 | P1 |
| C2 | C1 상태 | **차액지불** 체크 후 저장 | 잔여 검사 스킵, 저장 허용 | P1 |
| C3 | 증빙 프리패스권 보유 | 프리패스 사용 저장 | 할인권 **미차감** (베이스 B2 회귀) | **P1** |
| C4 | 50% 할인권으로 예매, 실물 미지참 | **낮은 할인으로 변경 + 차액 기록** | 적용 할인·차액이 지출 통계에 반영 (§DESIGN 4.5) | P2 |
| C5 | 빈 상태 | 웰컴키트 일괄 추가 | 40%·50%·프리패스·더블적립 각 1매 | P2 |
| C6 | 관람 4·6·8회 도달 | 재관람 카드 보상 처리 | 해당 할인권 자동 적립 | P2 |
| C7 | 프리패스 사용 기록 | 쿠폰 화면 확인 | 50%/40% "사용"으로 오집계 안 됨 (베이스 B7 회귀) | **P1** |

### D. 데이터 로딩

| ID | 조작 | 기대 결과 | P |
| --- | --- | --- | --- |
| D1 | 앱 최초 로드 | `schedule.json` 39회차 전부 캘린더 반영 | P1 |
| D2 | 이벤트 항목 클릭 | 해당 X 게시물로 이동 (`events[].url`) | P3 |
| D3 | 2차 스케줄 병합 후 로드 | 중복 회차 없음, 날짜순 정렬 유지 | P1 |
| D4 | 10/9(한글날) 확인 | 15:00·19:00 **2회차** 표시 | P2 |
| D5 | 10/5(월) 확인 | 유일한 월요일 공연으로 표시 | P2 |
| D6 | `schedule.json` 로드 실패 상황 | 앱이 죽지 않고 안내 표시 | P2 |

### E. 배역 5종 개조 회귀

베이스가 6배역이라 개조 시 가장 사고가 나기 쉬운 구간.

| ID | 조작 | 기대 결과 | P |
| --- | --- | --- | --- |
| E1 | 소스 전수 검색 | `magnus` `abel` `fredrick` `hagen` `jasper` `claire` **0건** | **P1** |
| E2 | 기록 폼 열기 | `rec-*` select 5개 모두 존재, `null` 참조 없음 | **P1** |
| E3 | 캘린더 배역 탭 | 탭 5개, 각 필터 정상 동작 | P2 |
| E4 | 통계 조합 표 | 5개 배역 열 + 회차·관람 열 렌더링 | P2 |
| E5 | 배역 탭 접기 | 필터 해제되어 회차 칩 유지 (베이스 B6 회귀) | P2 |

### U. 좌석·UI

| ID | 조작 | 기대 결과 | P |
| --- | --- | --- | --- |
| U1 | 층 선택 → 열 목록 | 1층 A~M(13개) / 2층 N~R(5개) | P2 |
| U2 | 열 선택 → 좌석번호 | `seating.json`의 `first`~`last` 범위와 일치 | P2 |
| U3 | 비판매석 입력 | 14석은 경고 또는 선택 불가 | P3 |
| U4 | 좌석 히트맵 | 1·2층 레이아웃이 실제 배치와 일치 | P3 |
| U5 | 다크/라이트 모드 | 5개 화면 모두 대비 정상 | P3 |

---

## 4. 실행 규약

| 시점 | 실행할 것 |
| --- | --- |
| 데이터(JSON) 수정 | L1 `python scripts/validate.py` |
| 로직 수정 | L1 + L2 `selftest.html` 전체 |
| 배역·좌석 구조 변경 | L1 + L2 + **E·U 전체** |
| 배포 전 | 3계층 전부 + P1 시나리오 수동 재확인 |

**실패 처리**: 실패한 시나리오는 고치고 끝내지 않고,
베이스 문서의 `🐛 B번호` 형식으로 원인·수정 내용을 남긴다.
같은 패턴의 버그가 반복되는지 추적하기 위함이다 — 실제로 베이스에서 B7·B8이 같은 패턴이었다.

---

## 5. 우선 착수 순서

1. **L1** — 이미 동작 중 ✅
2. **E1** — 개조 직후 즉시. 구 배역 키 잔존은 런타임 크래시로 직결
3. **R1·S1·S5·S6** — 사용자가 지목한 데이터 손실 위험 구간
4. **C3·C7** — 베이스에서 실제로 터졌던 버그의 회귀 방지
5. 나머지 P1 → P2 → P3
